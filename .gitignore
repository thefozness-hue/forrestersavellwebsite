# Build/upload artifacts — rebuild any time with:
#   cd site && zip -r ../preview-site.zip .
preview-site.zip
forrestersavell-golive.zip
*.zip

# Python bytecode
__pycache__/
*.pyc

# Local deploy credentials (never commit)
deploy.env
